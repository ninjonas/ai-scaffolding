---
name: test
description: "Run the full test suite for both Python and TypeScript."
allowed-tools: Bash(just *)
---

Run the full test suite for both Python and TypeScript:
`just test`
If tests fail, investigate the failures and fix them.
