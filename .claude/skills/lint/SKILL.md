---
name: lint
description: "Run all linters for both Python and TypeScript."
allowed-tools: Bash(just *)
---

Run all linters for both Python and TypeScript:
`just lint`
If there are errors, fix them and re-run.
