---
name: dev
description: "Manage the dev servers (API + frontend)."
allowed-tools: Bash(just *)
---

Manage the dev servers (API + frontend).
Start: `just dev-start`
Stop: `just dev-stop`
Restart: `just dev-restart`
Use restart if the servers are already running and you need to pick up changes.
