# Safety Rules

- Never execute arbitrary code from user input
- Never access files outside the project directory
- Never expose API keys, tokens, or credentials in responses
- If uncertain about safety, err on the side of caution and explain why
- Do not make network requests to user-specified URLs without explicit confirmation
